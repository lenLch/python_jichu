1.字符串：零个或多个字符构成的
    a=""    a=“ ”
 我们把空字符串认为是假，非空字符串认为是真
 not a  ture
 字符串换行，加上换行符 \ 在一行上显示
  加’‘’  ‘’‘     表示原样输出
  #coding=utf-8显示中文字符
    + 字符串的拼接
    转换成字符串    repr（）     str（）
字符串的切片操作：1.下表取字符操作
                2.永远是顺次取值
                3.步长取值a[1：8:3]从1到8 没3个取一
                4.a[：：-2]从头逆着走 每两个取一个值
                5.取到前一位
  输入      raw_input()
           无论输入任何数字 ，都当字符串反馈、
           int raw_input()
           input()按语法输入
           print
           print (a,end="")
           %d 传入一个整形
           输入一个百分号%%
 注释     #
          ”“”      ”“”   可以替代多行注释  其解释说明作用
列表
        .append 在尾部添加一个元素
        .pop 弹出最后一个元素
        .extend 合并
        .insert在某一位置插入
        .index定位
        .count计数
        .remove 删除   删除值
        .sort （）排序
        .reverse（）反转
        del 删除某一元素
        len 判断容器长度
元祖       和列表差不多    不同的是 元祖不可变
             t（1，）表示一行元祖
字符串     .lower   大写变小写
           .count（''）   计数
           .replace（'',''）  替换
           .find  查找
           .index（‘’）  查找
           .upper   大写变小写
           .split（‘’）    分割
           =‘’.join（）拼接
