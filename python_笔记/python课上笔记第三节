字典
   字典与列表不同通过键来实现元素存取，是一种无序集合，
   容器可变，元素也是可变的。
   键 值
   d{‘a’：1，‘b’：2，‘1’:3，‘c’：hello，‘d’：[1,2,3]}
   输入相应的建，取得相应的值
   字典的内部查找 称为散列查找
   字典中没有运算
      d.keys（）列出所有的键
       .values（）列出所有的值
       .items（）列出所有的键和值
       .get（‘’）获取相应的值
       .update（）将另一个字典合并在一起
       .pop（‘’）输入某一键弹出某一个值
       .del（‘’）删除某一值
     len（）查出几项
 集合
     不能定义成空s={},会被看成一个字典
     s= set（[]）一个空集合
     集合只能整体使用，不能取集合里的数
     s.add（）加入一个值
      .top（）弹出一个值
      .clear（）清空一个集合
      .remove（）删除集合中指定的某个数
    s.difference（s1）列出s中有s1中没有项
    s.union（s1）并集
    s.undate（s1）并集    集合中不允许有重复
      ........
     s= frozenset{}定义的集合是不可变的
   集合的运算
        支持多种运算
        s-s1   s中的元素减去与s1中相同的元素，s中所剩下的元素
        s|s1   s与s1的并集
        s&s1   s与s1的交集
        s^s1   s与s1的补集
        支持in
数据类型
   bool类型
           true   false  逻辑运算得到的结果
           notetype   none   用作空，站位变量
    数字类型
           整形                 不可变           数字运算位运算等
           浮点型                不可变           数字运算位运算等
           字符串类型   有序的     不可变          + * 运算        属性方法  切片操作    in逻辑
           列表        有序的     可变的          +  *  运算      属性方法  切片操作    in逻辑
           元祖        有序的     不可变          +  * 运算       属性方法  切片操作    in逻辑
           字典        无序的     可变的                          属性方法            in逻辑
           集合  set{} 无序的     可变的          -|&^运算         属性方法             in逻辑
         frozenset    无序的     不可变的         -|&^运算         属性方法            in逻辑
语句
    语句结构
        顺序结构
        分支（选择）结构  if  else  elif
        循环结构   for     in   while    else
              break    continue  用作循环控制
     if 语句
         0为假  空为假
     if else语句
         if     print
         else   print
    elif用法    if 不成立   运行 elif
                elif不成立    执行else
        if     elif     else
        
