python主要在web服务器编译，数据挖掘，自然语言处理，自动化测试等方面应用。

python主要在web服务器编译，数据挖掘，自然语言处理，自动化测试等方面应用。

print：输入（放入文件夹里的固定格式）

存储器：主存储器，外存储器，寄存储器

进制转换：
10进制转换2进制：10进制数除2  余数从下网上写
8进制：数字范围0-7；3位2进制算一位 在前面加0表示8进制
16进制：数字0-15；4位2进制算一位 在前面加0x表示16进制

查看ascii码：man ascii

把一个python文件成为一个模块，可能是系统写好的。
模块包含语句（可能是单独语句也可能是语句块）  语句包含表达式  表达式建立并处理对象

python程序是按着顺序执行，但是类，函数等语句需要调用后才能执行具体内容

关键字：系统当中由特殊含义的单词，他可以实现特定的功能
循环判断：
if（if语句关键字）         elif（elseif条件分支的关键字）
else（ifelse关键字）      for×（for循环关键字可能会搭配in使用）
while（while循环关键字）   break （跳出循环关键字）
continue（跳出本次循环）   and（逻辑与）   or（逻辑或）  not（逻辑非）
is （逻辑判读是否为同一对象） in（逻辑包含）

函数模块
from（引入什么内容）          inport（）
as（当做什么 一般用作生成对象） def（定义函数）
pass（空语句站位符）     lambda（定义匿名函数）
return（函数的返回值）     class（定义类）

异常：
try（异常检测）   except（异常处理）
finally（异常执行 最终的执行） raise（引发异常  抛出异常）

其他：
del（删除一个变量 对象）     global（声明使用全局变量）
with（形成一个对象）        assert（断言处理）
yield（函数的中断返回）      nonlocal（闭包的关键声明）
exec  print  （创建一个新的进程 python2 中是关键字）
true false none （逻辑真 假 空 是python3中的关键字）

核心数据类型
数字（整形int和浮点型float）   字符串（string） 列表（list） 字典（dict）
元祖（tuple） 集合（set）文件

其他类型：类类型  布尔型

内建函数：系统自带的函数
内存的最小存储单元是字节
id：每个字节的符号地址   作用：在同一地址的内容是一样的
type：显示数据的类型

常量：不能改变的量  （不能作为左值也就是等号的左边）
变量：认为设定有的标识符，用起来标记常量，可以被重复利用赋值

标识符命令的规则：
1标识符由一个或多个字母、数字或下划线组成
2.标识符的第一个字符必须是字母或下划线
3.标识符不能与关键字相同
4.python 语言是严格区分大小写的

标识符默认规则
1.以单一下划线开头的变量名（_x）不会被from module import * 语句导入
2.前后双下划线的变量(_x_)为系统的默认变量
3.前面两个下划线开头的变量(__x)为类的私有变量

python属于弱类型变量：赋值给变量是什么类型的值，变量就是什么类型  （没有类型区分的）

同时给变量赋值：a,b=1,2 用,隔开 （就是这三种）
两个数字交换：a,b=b,a (a ^= b) (b^=a) (a^=b)(只对整数有用);

四则运算：
除法是取整除法（取商） % 取余
当出现小数时会自动当成小数运算对于取余python不允许都出现小数
python3：中/是真除法（支持小数运算） //和p0ython2中/是一样的2

位运算：
a<<2 a左移 2位  表示在最右边加两个00  相当于乘4
a>>2 a
右移 2位  表示删除最右边的两个数  相当于除以4取整
a | b （与） 两个二进制数比较只要有一个数是1就取1
a & b （或） 两个二进制数比较只要由一个数是0就取0
a ^ b （异或） 两个二进制数比较相同为0不同为1  （只对整数有效）
～a     把a转化成2进制数 把0换成1 1换成0  通常用2进制数

逻辑运算： 要么真要么假  false 0（假） True 1（真）
== （!=） or(或)  and（与） not（非） is：判断是否为同一对象 （is not）
in：判断某数是否在数组里 （not in）
支持连续逻辑运算：a>b>c
或（or）：一真则真   与（and）：一假则假  非（not）：真变假  假变真
a += 1 ——————>  a = a + 1

强制装换：
int（a）： 转换成整数  a=1.234 int（a）= 1
数据库：import math dir（math）（引入数学库）  pow （3,2 是3 的 2 次方） sqrt （开方）

a  b  c  分别附一个整数值 求这三个数组成的三角形的面积
s = (a+b+c)/2  面积=(s*(s-a)×(s-b)×(s-c)) 开方
第二部分   第5章和第6章
